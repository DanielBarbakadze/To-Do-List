import React from 'react';

export default function Login() {
  
  return (
  <div>
    <header className="modeContainer">
        <button className="modeButton">Light Mode</button>
        <button className="modeButton">Dark Mode</button>
    </header>
    <section className="loginBox">
      <label>Log In</label>
    </section>
  </div>
  )
}